version: '3.8'
services:
  twindeploy:
    build: .
    ports:
      - "9547:9547"
    environment:
      - NODE_ENV=production
    # Uncomment and set these if needed
    # env_file:
    #   - backend/.env
    # volumes:
    #   - ./backend:/app/backend
